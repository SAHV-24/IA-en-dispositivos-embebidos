// EdgeImpulse: aquí irá el modelo y la inferencia
// Mantener vacío por ahora, solo firma opcional de ejemplo.

// void run_inference() {
//   // usar sendInferenceResult("A", 0.92f);
// }

// Edge Impulse inference (stub)

void runInference() {
  // aquí iría el modelo de EI; por ahora enviar un resultado genérico
  sendInferenceResult("A", 0.950f);
}
